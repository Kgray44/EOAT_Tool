[pytest]
markers =
    usability: realistic EOAT Command Center usability and workflow tests using fake project data.
    integration: cross-page or cross-tool integration tests.
    slow: longer end-to-end workflows.
testpaths = tests
